<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="res_config_settings_view_form" model="ir.ui.view">
        <field name="name">res.config.settings.view.form.inherit.members</field>
        <field name="model">res.config.settings</field>
        <field name="inherit_id" ref="website.res_config_settings_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//block[@id='website_info_settings']" position="after">
                <block title="Members" id="member_settings">
                    <setting string="Members intro text" help="Shown above the member cards.">
                        <field name="member_intro"/>
                    </setting>
                    <setting string="Anonymous visitors" help="What happens when a guest opens /membres.">
                        <field name="member_deny_redirect"/>
                    </setting>
                </block>
            </xpath>
        </field>
    </record>
</odoo>
